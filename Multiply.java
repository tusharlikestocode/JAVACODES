public class Multiply {
    public static void main(String h[]) {
        int n1=Integer.parseInt(h[0]);
        int n2=Integer.parseInt(h[1]);
        int n3=Integer.parseInt(h[2]);
        System.out.println("the new numbers "+(n1*5)+" "+(n2*5)+" "+(n3*5));

    }
}
